apiVersion: v1
kind: Pod
metadata:
  name: env-test 
spec:
  containers:
    - name: busybox 
      image: busybox 
      command: [ "/bin/sh", "-c", "env" ]
      envFrom:
      - configMapRef:
          name: app-config 
  